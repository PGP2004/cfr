Study Stack:
Read more about git - https://git-scm.com/book
Callables - https://csiflabs.cs.ucdavis.edu/~tyfeng/ecs34/C++/callable.html


TODO: 
- Set off a fable review and fix the issues it raises
- Unslop the Preflop helpers 
- Split the data analysis up and also unslop it a bit
- Cleaner Training Script -> 

- Make it MultiThreaded and speed up the clustering <- Finish!

struct CFRConfig{
        std::optional<ISetsCkpt> iset_ckpt;
        BucketPaths bucket_paths;
        std::vector<std::vector<float>> bet_sizes;
};

struct TrainConfig{
    int num_iters;
    int discounts_per_iter;
}

struct LogConfig{
    fs::path data_folder;
    std::string run_name;
    std::vector<int> preflop_ckpts;
    bool log_infosets;
}

